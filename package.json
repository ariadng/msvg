{
  "name": "msvg-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18.17"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=msvg-schema && npm run build --workspace=msvg && npm run build --workspace=msvg-cli && npm run build --workspace=msvg-react",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "happy-dom": "^15.11.0",
    "typescript": "^5.6.3",
    "vitest": "^3.0.0"
  }
}
