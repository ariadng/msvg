# Delete

An animated delete (trash can) icon as a MotionSVG package.

- **intro** — fades and drops in from above into its resting state.
- **loadingLoop** — a gentle pendulum tilt, rotating from -6deg to +6deg and back, so the first and last keyframes match for a seamless repeat.

Icon from Google Material Symbols (Apache-2.0).
