# Notifications

An animated notifications (bell) icon as a MotionSVG package.

- **intro** — fades and eases up into its resting state.
- **loadingLoop** — a bell swing: a rotation arc whose amplitude decays across the keyframes and returns to 0deg, so the loop repeats seamlessly.

Icon from Google Material Symbols (Apache-2.0).
