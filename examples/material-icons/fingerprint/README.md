# fingerprint

An animated msvg package for the Material Symbols **fingerprint** icon.

- **Loop concept:** A fingerprint that dims and swells in one slow biometric scan pass.
- **Timelines:** `intro` (fade + settle in), `hover`, `loadingLoop` (seamless, meaningful gesture + rest).

Icon from Google Material Symbols (Apache-2.0).
