# Person

An animated person icon as a MotionSVG package.

- **intro** — fades and eases upward into its resting state.
- **loadingLoop** — a calm breathing motion: a subtle scale up paired with a tiny vertical bob, returning to scale(1) at translateY(0) so the loop repeats seamlessly.

Icon from Google Material Symbols (Apache-2.0).
