# Shopping Cart

An animated shopping cart icon as a MotionSVG package.

- **intro** — rolls in from the left while fading up to its resting state.
- **loadingLoop** — a rolling nudge: the cart drifts right with a small bounce, then returns to translateX(0) so the loop repeats seamlessly.

Icon from Google Material Symbols (Apache-2.0).
